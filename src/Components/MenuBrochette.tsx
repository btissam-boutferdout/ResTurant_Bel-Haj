import MenuData from "../Pages/MenuData";
import MenuSection from "./MenuSection";

const MenuBrochette = () => {
  return <MenuSection title="Brouchette" menuItem={MenuData.Brouchette} />;
};

export default MenuBrochette;
