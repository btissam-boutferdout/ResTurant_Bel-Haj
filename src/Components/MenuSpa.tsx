import MenuData from "../Pages/MenuData";
import MenuSection from "./MenuSection";

const MenuSpa = () => {
  return <MenuSection title="Spaghitti" menuItem={MenuData.Spa} />;
};

export default MenuSpa;
