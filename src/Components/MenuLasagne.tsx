import MenuData from "../Pages/MenuData";
import MenuSection from "./MenuSection";

const MenuLasagne = () => {
  return <MenuSection title="Lasagne" menuItem={MenuData.Lasagne} />;
};

export default MenuLasagne;
