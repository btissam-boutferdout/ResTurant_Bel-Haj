import MenuData from "../Pages/MenuData";
import MenuSection from "./MenuSection";

const MenuBurger = () => {
  return <MenuSection title="Burger" menuItem={MenuData.Burger} />;
};

export default MenuBurger;
