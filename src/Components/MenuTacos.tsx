import MenuData from "../Pages/MenuData";
import MenuSection from "./MenuSection";

const MenuTacos = () => {
  return <MenuSection title="Tacos" menuItem={MenuData.Tacos} />;
};

export default MenuTacos;
